// Kode ini berfungsi sebagai placeholder untuk generate OpenGraph image
// Dalam produksi sebenarnya, gunakan layanan seperti @vercel/og, atau buat gambar statis berkualitas tinggi
// Contoh dengan @vercel/og akan seperti ini:

/*
import { ImageResponse } from '@vercel/og';
 
export const runtime = 'edge';
 
export async function GET(request: Request) {
  return new ImageResponse(
    (
      <div
        style={{
          display: 'flex',
          fontSize: 60,
          color: 'white',
          background: '#2E7D32',
          width: '100%',
          height: '100%',
          padding: '50px 200px',
          textAlign: 'center',
          justifyContent: 'center',
          alignItems: 'center',
        }}
      >
        <h1>DesaConnect</h1>
        <p>Platform Aspirasi Masyarakat Desa</p>
      </div>
    ),
    {
      width: 1200,
      height: 630,
    },
  );
}
*/

console.log('Harap buat file gambar OpenGraph dan Twitter card yang sebenarnya di lokasi berikut:');
console.log('- public/images/og-image.png (1200x630px)');
console.log('- public/images/twitter-card.png (1200x600px)'); 